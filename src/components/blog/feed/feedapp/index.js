import React from 'react';

const Feed = (props) => (
  <section className="Feed">
    <Header page="Feed" />
    <Subnav variation="Feed"/>
    <div className={styles.wrapper}>
      <h2>Feed</h2>
    </div>
  </section>
);
