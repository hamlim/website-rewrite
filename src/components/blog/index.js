import React from 'react';

import Anchor from '../anchor';
import Header from '../header/header';

const Blog = (props) => (
  <div className="Blog">
    <Header page="Blog" />
  </div>
);


export default Blog;
